apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-app.fullname" . }}
  labels:
    {{- include "transfer-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transfer-app.selectorLabels" . | nindent 4 }}
