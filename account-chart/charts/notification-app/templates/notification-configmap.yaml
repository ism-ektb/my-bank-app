apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-configmap  # Имя ConfigMap будет комбинироваться с именем релиза
data:
  KEYCLOAK.URL: {{ tpl .Values.service_url.keycloak_url . | quote}}
