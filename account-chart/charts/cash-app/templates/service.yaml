apiVersion: v1
kind: Service
metadata:
  name: {{ include "cash-app.fullname" . }}
  labels:
    {{- include "cash-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cash-app.selectorLabels" . | nindent 4 }}
