apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash-app.fullname" . }}-test-connection"
  labels:
    {{- include "cash-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cash-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
