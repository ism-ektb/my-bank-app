server:
  port: 8085

spring:
  application:
    name: my-bank-transfer-app
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/bank-realm
      client:
        registration:
          transfer-app:
            provider: keycloak
            client-id: transfer-app
            client-secret: L3Vf34xlCdZIAaNou6jPb4KwQBCnL4R9
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/bank-realm

management:
  endpoints:
    web:
      exposure:
        include: "*"

bank:
  accounts-service:
    base-url: http://localhost:8083

logging:
  level:
    root: INFO
    org.springframework:
      security: trace

